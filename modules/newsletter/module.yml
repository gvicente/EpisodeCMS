intro:
  en:
    title: Newsletter
menu:
  admin:
    Content:
      Newsletter:
        _link:
          controller: admin
          action: browse
          module: newsletter
          model: subscriber
routes:
  /newsletter:
    controller: newsletter
    action: subscribe
models:
  Subscriber:
    _default:
    name: string
    email: string
    verfied: bool
