intro:
  version: 1.0.1
  en:
    title: Newsletter
menu:
  admin:
    Content:
      Newsletter:
        _link:
          controller: admin
          action: browse
          module: newsletter
          model: Subscriber
routes:
  /newsletter:
    controller: newsletter
    action: subscribe
models:
  Subscriber:
    _default:
    name: string
    email: string
    verfied: bool
